CREATE SEQUENCE IF NOT EXISTS hibernate_sequence START WITH 1 INCREMENT BY 1;
CREATE TABLE IF NOT EXISTS TODO(id BIGINT PRIMARY KEY, title VARCHAR, done BOOLEAN, created_at TIMESTAMP, updated_at TIMESTAMP);